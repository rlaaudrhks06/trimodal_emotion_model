# 작업 절차

반려 로봇용 한국어 트리모달 감정인식. 채택 모델 **v11** — 7클래스 46.19%,
3클래스 67.34%, 화자 독립. 상세는 `README.md`와 `docs/`.

이 문서는 **규칙 목록이 아니라 순서**다. 규칙은 절차의 각 단계에 붙어 있다.
어느 단계인지 알면 어떤 규칙이 적용되는지도 정해진다.

**기계가 막는 것은 여기 없다.** `.claude/hooks/guard.py`와 `.gitignore`가 강제한다.
아래는 판단이 필요해서 기계가 못 막는 것뿐이다. 각 항목에 **[사건]**(왜 생겼나)과
**[폐기]**(언제 죽나)를 붙인다 — 사건이 없으면 잘못 따르고, 폐기 조건이 없으면 낡는다.

---

## 본 절차

```
   작업 요청
      │
  [0] 규칙을 바꾸는 일인가? ──── 예 ──▶ 규칙 변경 절차로 (아래)
      │ 아니오
  [1] 브랜치를 판다                        ← 가드가 강제
      │
  [2] 건드릴 것을 먼저 읽는다
      │
  [3] 한 번에 하나만 바꾼다
      │
  [4] 검증한다 ★ 여기서 제일 많이 실패했다
      │
  [5] 기록한다
      │
  [6] 보고한다
      │
  [7] 머지·push ─────────────────▶ 사용자 결정  ← 가드가 강제
```

### [1] 브랜치를 판다

`git checkout -b <작업이름> main`. 문서(`docs/` 아래·모든 `*.md`)는 예외로 main에서 바로 고친다.

### [2] 건드릴 것을 먼저 읽는다

- **바꿀 함수의 호출부를 전부 찾는다.** 한 곳만 고치면 형제 호출부가 남는다.
  [사건] "오디오를 끈다"가 여러 곳에 흩어져 있어 같은 버그가 **두 번** 났다
  (멜만 지움 → 파형 안 지움).
  [폐기] 없음.
- **백본·라이브러리를 의심하기 전에 입력을 눈으로 본다.**
  [사건] 백본을 세 번 갈아치웠는데 `face_frames_dir`에 든 건 얼굴이 아니라 전신이었다.
  [폐기] 없음.
- **논문·공식 설정을 근거로 삼을 땐 원문을 연다.**
  [사건] XLSR의 `preprocessor_config.json`을 열고서야 정규화 누락을 알았다.
  late fusion 베이스라인도 MulT 원문을 보고 MLP→self-attention으로 바꿨다.
  [폐기] 없음.

### [3] 한 번에 하나만 바꾼다

- 애블레이션 config는 v11에서 **한 항목만** 다르게 만든다.
  [사건] v3→v4에서 세운 원칙. 두 개를 같이 바꾸면 어느 쪽 효과인지 못 가른다.
  [폐기] 없음.
- **`batch_size: 128` 고정.** 낮추면 에폭당 스텝이 443→886으로 2배가 되어
  "같은 에폭 수"가 같은 학습량이 아니게 된다.
  [사건] 8.18절에서 128→64로 낮췄다가 되돌렸다.
  [폐기] v11과 비교하지 않게 되면.
- **`split_manifest.py`는 `--unit speaker --seed 2026 --verify`.** 기본값 `clip`은
  화자 누수를 만든다.
  [사건] test 화자 278명 전원이 train에도 있었다. 실질 6.51%p가 허수였다.
  [폐기] 기본값이 speaker로 바뀌면.

### [4] 검증한다 ★

**코드를 의심하는 만큼 확인 방법도 의심한다.** 아래 넷은 **하루에 다 일어났다** —
코드는 멀쩡했고 확인 방법이 틀렸다.

```
   결과가 나왔다
      │
   4-1  "없음 / 0건 / 전부 통과" 인가?
        └─ 예 ──▶ 양성 대조를 같은 명령에 넣어 검색이 살아 있음을 보인다
      │
   4-2  N개를 1개로 줄였는가? (평균·합·최대)
        └─ 예 ──▶ "이 축으로 줄이면 무엇이 사라지나" 한 줄 적는다. 답이 없으면 분해한다
      │
   4-3  백그라운드였는가?
        └─ 예 ──▶ 종료 코드 말고 출력 파일을 읽는다
      │
   4-4  정규식으로 찾았는가?
        └─ 예 ──▶ 경계를 앵커하고, 매칭된 실제 줄을 눈으로 본다
      │
   통과 ──▶ [5]
```

- [4-1 사건] 민감정보 grep이 "없음"을 냈는데 검색이 고장난 건지 구분이 안 됐다.
  `--help` 6개가 "전부 실패"로 나온 것도 같은 구조였다(zsh 단어분리).
- [4-2 사건] 운율 게이트 512차원의 평균을 보고 "안 움직인다"고 결론냈다. 상쇄된
  값이었고 분해하니 실제 변동은 7.7배였다. 8.29.1의 통계 오류도 분모를 잘못 고른 것이다.
- [4-3 사건] 출력이 빈 채로 exit 0이 보고됐다.
- [4-4 사건] `13\.[0-9]절`이 `8.13.2절`을 잡아 깨진 참조로 보고할 뻔했다.
- [폐기] 넷 다 없음.

**수치로 결론 낼 때:**

- **1.3%p 미만 차이로는 결론을 내지 않는다.** 두 모델 *차이*의 표준오차가 약 0.66%p다.
  [사건] 단일 모델 표준오차(0.47%p)로 나눠 "유의"가 "구분 안 됨"으로 뒤집혔다(8.29.1).
  [폐기] test 세트 크기가 바뀌면 다시 계산한다.
- **평가는 항상 `--save-predictions`.**
  [사건] v11과 v12b는 지금도 McNemar 검정을 못 한다.
  [폐기] 없음.
- **효과 크기가 1.3%p 근처면 시드를 2~3개 잡는다.**
  [사건] 선행 연구의 교차 어텐션 기여가 1.77~2.8%p로 검출 한계에 가깝다.
  [폐기] 해당 실험이 끝나면.

**지우는 코드를 쓸 때:**

- **`rm`·`rmtree` 전에 지울 경로와 지킬 경로의 관계를 적는다.** 한쪽이 다른 쪽의
  조상이면 멈춘다.
  [사건] 임시 디렉터리를 최종 디렉터리 **안**에 만들고 최종 디렉터리를 비웠다.
  검증을 다 통과한 산출물이 마지막 줄에서 사라졌다.
  [폐기] 없음.

### [5] 기록한다

- **수치를 손으로 옮기지 않는다.** 표는 스크립트가 원본에서 읽어 만든다.
  [사건] 모델카드 train_loss 6줄이 옮겨 적다 틀렸고, 대시보드에 애블레이션 값을
  상수로 박아뒀다.
  [폐기] 없음.
- **"아직 안 했다"는 통합기록 10장에만 쓴다.** 다른 문서는 그곳을 가리킨다.
  [사건] 미실행 목록이 4개 문서에 흩어져 8.30절 실행 후 결과 절만 갱신됐다.
  같은 문서 안에서 앞뒤가 모순된 곳이 6군데였다.
  [폐기] 없음.
- **결과를 적기 전에 그 항목이 "미실행"으로 적힌 곳을 grep한다.**
  [사건] 위와 같음. [폐기] 없음.

### [6] 보고한다

- **"확인했다"에 확인 방법을 괄호로 같이 쓴다.**
  [사건] `--help` 6개가 "전부 실패"로 나왔는데 방법이 틀려서 결론도 틀렸다.
  방법이 적혀 있으면 나중에 방법부터 볼 수 있다.
  [폐기] 없음.

### [7] 머지·push

가드가 막는다. 무엇을 왜 하려는지 보고하고 승인을 받는다.

---

## 실패가 났을 때

```
   실패 발견
      │
   같은 유형이 처음인가?
      │
      ├─ 예 ──▶ 고치고, 어디서 왜 났는지 기록한다
      │
      └─ 아니오(2회째) ──▶ 문서에 한 줄 더 쓰지 않는다
                            assert · 테스트 · .gitignore · 훅 중 하나를 만든다
                            (가드를 만드는 것이면 규칙 변경 절차를 탄다)
```

[사건] 이슈기록 15장: *"교훈을 문서에 적는 걸로는 안 막힌다. 실제로 이 문서에 그
교훈을 쓰는 자리에서 또 어겼다. 구조로 막아야 한다."* 모달리티 드롭아웃 버그는
두 번 났고 구조는 아직 그대로다.
[폐기] 없음.

---

## 서브 세션 절차 — 작업 중 다른 일이 들어왔을 때

```
   작업 도중 다른 요청이 들어왔다
      │
   ① 지금 작업과 **같은 파일**을 건드리나?
      └─ 예 ─▶ 서브 세션을 만들지 않는다. 순서대로 한다 (충돌이 확실하다)
      │ 아니오
   ② 새 요청이 **학습·평가·체크포인트 쓰기**를 하나?
      └─ 예 ─▶ 만들지 않는다. checkpoints가 링크라 원본과 같은 곳에 쓴다
      │ 아니오 (코드·문서·분석만)
   ③ 만든다
      │     bash .claude/hooks/worktree_new.sh <작업이름> [분기점]
      │     .venv·models·checkpoints·data·embeddings를 링크까지 걸어준다
      ▼
   ④ 그 트리에서 본 절차 [1]~[6]을 그대로 밟는다
      ▼
   ⑤ 테스트 통과 → 커밋
      ▼
   ⑥ 머지          🔒 가드: ask (사용자 결정)
      ▼
   ⑦ git worktree remove <경로>
```

**`git worktree add`만 하면 안 된다.** 이 저장소는 `.venv`(1.8G)·`models`(1.2G)·
`checkpoints`(4.8G)·`data`(27G)가 전부 gitignore라 새 트리에 **따라오지 않는다.**
링크를 잊으면 테스트도 Stop 게이트도 못 돌고, 그러면 "검증했다"가 거짓이 된다.
그래서 `worktree_new.sh`가 링크까지 건다.

**몇 개까지?** 파일이 겹치지 않는 만큼만. 겹치기 시작하면 worktree가 해결하는 게
아니라 문제를 옮기는 것이다.

[사건] 이 절차를 만들면서 훅 두 개의 버그를 찾았다 — `stop_gate.sh`가 경로를
하드코딩해 worktree에서 **원본 트리를 검사**했고(엉뚱한 코드를 통과시킨다),
`guard.py`가 절대경로로만 판정해 **worktree의 규칙 파일이 무방비**였다. 둘 다 고쳤다.
[폐기] 저장소가 데이터·가상환경을 git에 넣게 되면(그럴 일은 없다).

---

## 규칙 변경 절차

**규칙이 작업을 방해하면 고치는 게 맞다. 다만 고칠지는 사용자가 정한다.**

```
   규칙에 막혔다 / 규칙이 틀렸다고 판단된다
      │
   ① 무엇이 막혔는지, 왜 규칙이 틀렸는지 1~2줄로 보고
      │
   ② 사용자 승인 ──▶ 사용자가 실행:  touch .claude/.rule-change-approved
      │
   ③ 규칙 파일 수정 (토큰은 1회용 — 쓰는 즉시 사라진다)
      │
   ④ [사건]과 [폐기] 줄을 갱신한다
      │
   ⑤ 무엇을 왜 바꿨는지 보고
```

규칙 파일: `CLAUDE.md` · `.claude/hooks/*` · `.claude/rules/*` · `.gitignore`

**규칙이 손해를 냈다면 그 자체가 [사건]이다.** 다음 규칙의 근거가 되므로 반드시 적는다.

---

## 명령

```bash
python tests/test_forward_smoke.py                                   # 스모크 5종
python scripts/train.py --config configs/config_si_w2v.yaml          # 학습 (v11 설정)
python scripts/evaluate.py --config configs/config_si_w2v.yaml \
    --checkpoint <ckpt> --save-as <이름> --save-predictions          # 평가
python scripts/export_onnx.py --checkpoint checkpoints/v11_best.pt \
    --out checkpoints/onnx/v11.onnx                                  # ONNX + 동등성 검증
```
