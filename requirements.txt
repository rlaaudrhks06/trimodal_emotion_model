torch>=2.2
torchvision>=0.17
emotiefflib>=1.1.1
transformers>=4.40
librosa>=0.10
soundfile>=0.12
opencv-python>=4.9
av>=13.0
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
scipy>=1.11
pyyaml>=6.0
matplotlib>=3.8
mediapipe>=0.10

# 젯슨 배포용 ONNX 변환(scripts/export_onnx.py)에만 쓴다 — 학습에는 불필요하다.
# 그래도 명세에 적는 이유: 직접 의존성을 적어두지 않아 서버에서 다른 버전이 설치되던
# 문제를 이미 겪었다(제안서 4.3 ⑦). onnxscript는 torch 2.9+ dynamo exporter가 요구한다.
onnx>=1.17
onnxruntime>=1.20
onnxscript>=0.1
