# ⚠️ 이 파일은 v4 당시(8클래스) 설정을 남긴 기록용이며 **지금 코드로는 실행되지 않는다**.
#   8.10절에서 경멸을 혐오에 병합해 src/datasets/labels.py가 7클래스로 고정됐기 때문에,
#   num_classes=8로 학습을 걸면 클래스 가중치(7개)와 로짓(8개)이 안 맞아 첫 배치에서 죽는다.
#   v4를 재현하려면 config가 아니라 labels.py의 LABEL_MERGE를 되돌려야 한다.
# config.yaml(과적합 대응 2차 학습)과 완전히 동일하되, prosody 정규화만 추가로 켠
# A/B 비교용 설정. 데이터_전처리_EDA_점검_및_근거.md §1.2/§4-1,2 참고.
#
# 먼저 scripts/compute_prosody_stats.py로 data/prosody_stats_train.json을 만든
# 뒤에 이 설정으로 학습해야 한다. checkpoint_dir/periodic_checkpoint_dir을 다르게
# 줘서 지금 돌고 있는 baseline(config.yaml, checkpoints/)을 덮어쓰지 않는다.
#
# 주의(사후 기록): archived_runs/checkpoint_v4_best는 이 파일로 학습됐지만,
# 당시 영상 백본은 사전학습 없는 소형 CNN이었다. 이후(8.7절 베이스라인 결과로)
# src/models/visual_backbone.py를 MobileNetV3-Small 사전학습 백본으로 교체했는데,
# 이 파일을 그대로 재사용하면(visual_freeze_layers 미지정 시 기본값 9 적용)
# 그 v4 체크포인트와 다른 아키텍처가 된다 — v4 체크포인트는 새 코드로 로드 불가.
model:
  d_model: 256
  n_heads: 8
  ffn_dim: 1024
  backbone_layers: 2
  num_classes: 8
  prosody_dim: 10
  cross_attn_dropout: 0.2
  backbone_dropout: 0.2
  visual_cnn_dropout: 0.15
  classifier_dropout: 0.35
  text_dropout: 0.2
  text_freeze_layers: 8

text:
  pretrained_model: "klue/bert-base"

audio:
  sample_rate: 16000
  n_mels: 80
  n_fft: 400
  hop_length: 160

visual:
  face_size: 112

train:
  batch_size: 128
  epochs: 100
  lr: 0.0002
  weight_decay: 0.05
  modality_dropout_prob: 0.25
  label_smoothing: 0.1
  lr_scheduler_patience: 3
  lr_scheduler_factor: 0.5
  num_workers: 8
  feature_cache_dir: "data/feature_cache"
  train_manifest: "data/manifests/train.csv"
  val_manifest: "data/manifests/val.csv"
  test_manifest: "data/manifests/test.csv"
  # --- 이 파일에서만 다른 부분 ---
  prosody_stats_path: "data/prosody_stats_train.json"   # 신규: prosody z-score 정규화 켬(train-only fit)
  checkpoint_dir: "checkpoints_prosody_norm"                   # baseline과 겹치지 않게 분리
  periodic_checkpoint_dir: "checkpoints_periodic_prosody_norm"
  checkpoint_interval: 20

